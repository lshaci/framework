################(RedisProperties)
# Maximum number of redirects to follow when executing commands across the cluster.
#spring.redis.cluster.max-redirects=
# Comma-separated list of "host:port" pairs to bootstrap from.
spring.redis.cluster.nodes=104.207.134.95:7000,104.207.134.95:7001,104.207.134.95:7002
# Database index used by the connection factory.
spring.redis.database=0
# Connection URL, will override host, port and password (user will be ignored), e.g. redis://user:password@example.com:6379
spring.redis.url=
# Redis server host.
#spring.redis.host=104.207.134.95
# Login password of the redis server.
spring.redis.password=
# Redis server port.
#spring.redis.port=7000
# Connection timeout in milliseconds.
spring.redis.timeout=0
# Enable SSL support.
spring.redis.ssl=false
# Max number of connections that can be allocated by the pool at a given time. Use a negative value for no limit.
spring.redis.pool.max-active=8
# Target for the minimum number of idle connections to maintain in the pool. This setting only has an effect if it is positive.
spring.redis.pool.min-idle=0
# Max number of "idle" connections in the pool. Use a negative value to indicate an unlimited number of idle connections.
spring.redis.pool.max-idle=8
# Maximum amount of time (in milliseconds) a connection allocation should block before throwing an exception when the pool is exhausted. Use a negative value to block indefinitely.
spring.redis.pool.max-wait=-1
# Name of Redis server.
#spring.redis.sentinel.master=
# Comma-separated list of host:port pairs.
#spring.redis.sentinel.nodes=

# Redis locker default expire time(millisecond)
#locker.redis.defaultExpireTime=5000

# Redis distributed lock default retry count of get lock(int)
#distributed.lock.redis.defaultRetryCount=1
# Redis distributed lock default delay time of get lock(millisecond)
#distributed.lock.redis.defaultDelayTime=100
# Redis distributed lock default expire time(millisecond)
#distributed.lock.redis.defaultExpireTime=5000




