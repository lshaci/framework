package ${servicePackage};

import ${mapperPackage}.${domainName}Mapper;
import ${domainPackage}.${domainName};

import top.lshaci.framework.service.BaseService;

/**
 * ${domainName} service interface
 *
 * @author lshaci
 */
public interface ${domainName}Service extends BaseService<${domainName}, ${domainName}Mapper> {

}