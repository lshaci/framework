# spring
spring:
  # datasource
  datasource:
    # druid
    druid:
      url: jdbc:mysql://host:port/database?useUnicode=true&characterEncoding=utf8
      username:
      password:
# framework
framework:
  # swagger
  swagger:
    enabled: false
