# spring
spring:
  # datasource
  datasource:
    # druid
    druid:
      url: jdbc:mysql://host:port/database?${mysql.url.params}
      username:
      password:
# logging
logging:
  level:
    ${groupId}.${artifactId}: debug
# logs
logs:
  path: ./logs
